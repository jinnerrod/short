# Puerto del servidor
PORT=4321

# Contraseña del administrador (texto plano, se compara con hash SHA-256 interno)
ADMIN_PASSWORD=cambia_esta_contrasena

# Entorno (development | production)
# En producción las cookies se marcan como secure (HTTPS obligatorio)
NODE_ENV=development
