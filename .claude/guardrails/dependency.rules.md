# Dependency Rules

Claude must avoid adding unnecessary dependencies.

Allowed libraries:

- express
- better-sqlite3-multiple-ciphers
- argon2
- dotenv
- express-session

If new dependency is required:

1. Explain why
2. Provide alternatives
3. Wait for confirmation